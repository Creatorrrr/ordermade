<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
	<properties resource="dataSource.properties"></properties>
	
	<typeAliases>
		<!-- write typeAlias -->
		<typeAlias type="ordermade.domain.Product" alias="Product"/>
		<typeAlias type="ordermade.domain.InviteRequest" alias="InviteRequest"/>
		<typeAlias type="ordermade.domain.Request" alias="Request"/>
	</typeAliases>
	
	<environments default="development">
	  	<environment id="development">
	  		<transactionManager type="JDBC"/>
	  		<dataSource type="POOLED">
	  			<property name="driver" value="${driver}"/>
	  			<property name="url" value="${url}"/>
	  			<property name="username" value="${username}"/>
	  			<property name="password" value="${password}"/>
	  		</dataSource>
	  	</environment>
	</environments>
	
	<mappers>
		<mapper resource="store/mapper/ProductMapper.xml"/>
		<mapper resource="store/mapper/InviteRequestMapper.xml"/>
		<mapper resource="store/mapper/RequestMapper.xml"/>
	</mappers>
</configuration>